# Method

Add the method description here. Inline math should use \(x_\ell\), and display
math should use block equations:

$$
x_{\ell+1} = x_\ell + \mathrm{Attn}_\ell(x_\ell) + \mathrm{MLP}_\ell(x_\ell).
$$
