# NeurIPS 2026 workshop submission (v35) — frozen

This folder is the exact paper submitted to the NeurIPS 2026 workshop. Do not
edit, re-render into, or replace anything here. The ICLR 2027 main-track
extension is developed separately (render target `iclr`, output under
`dist/iclr-submission/`).

| File | Origin |
|---|---|
| `paper.pdf` | `dist/neurips-submission/neurips-planning-trajectories-Gemma-3-270M-v35.pdf`, the submitted PDF |
| `paper.tex` | Generated LaTeX for that build |
| `build-manifest.json` | Original build manifest (2026-09-02T11:49:24Z) |

The PDF and `.tex` hashes match `pdf_sha256` and `tex_sha256` in the manifest
(see `SHA256SUMS`). The manuscript source for this version is Git tag
`neurips-2026-workshop-v35` (commit `e7e1372`). Rendering that commit with
`bin/render-paper neurips submission` reproduces `paper.tex` byte for byte;
the rebuilt PDF differs only in embedded build timestamps.
