# Independent headline-number reproduction

This compact script has no model dependency and does not import any experiment
runner. It recomputes the three headline aggregates directly from immutable row
files: S1's six-cell vector magnitudes, the prompt-aligned boundary result, and
the pre-article target-minus-random effect.

```bash
/Users/anthony/miniconda3/bin/python experiments/headline_reproduction/run.py
```
